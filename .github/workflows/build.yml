name: Android Builds

on:
  push:
    branches:
      - '*'
      - '!master'
      - '!release*'

jobs:
  build:
    name: Build Example
    runs-on: ubuntu-latest

    steps:
    - name: Build
    - run: ./gradlew :example:assembleDebug
